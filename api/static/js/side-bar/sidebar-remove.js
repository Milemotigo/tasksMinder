$(document).on('click', function(event) {
	let sidebar = $('#sidebar')
	let sidebarEl = ($(event.target)
	if ((!sidebarEl) && (sidebar).hide()) {
		sodebar.show()
	}
		else {
		    button = document.getElementById('s-button').show()
		}
}
});

